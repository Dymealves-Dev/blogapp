{{#each errors}}
<div class="alert alert-danger">{{this.text}}</div>
{{else}}

{{/each}}
<div class="jumbotron">
  <h1 class="display-4">Bem-vindo ao blog de notícias do Podpah!</h1>
  <p class="lead">Esse blog foi feito para levar as novidades do Podpah até você em um piscar de olhos.</p>
  <hr>
{{#if user}}

{{else}}
  <p>Crie sua conta agora e seja sempre o primeiro a receber as novidades.</p>
  <p class="lead">
    <a class="btn btn-warning btn-lg" href="/user/register" role="button">Crie sua conta</a>
  </p>
</div>

<hr>
{{/if}}

{{#each posts}}
<div class="card mt-3">
    <div class="card-body">
        <h1>{{this.title}}</h1>
        <p>{{this.description}} <a href="/posts/{{this.slug}}" class="text-decoration-none">Leia mais</a></p>
        <hr>
        <small>Categoria: {{this.category.name}}</small>
        <br>
        <small>Data de Públicação: {{this.date}}</small>
    </div>
</div>
{{else}}
<span class="m-2">Não há nenhuma postagem recente.</span>
{{/each}}